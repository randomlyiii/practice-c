# 手写 C 副线练习清单 (嵌入式笔试 / 面试八股)

用途: 副线练 C 手写能力。力扣用 C++ 刷, 这里用**纯 C** 手写数据结构 + 算法, 不依赖 STL。

每个文件 = 一个主题 + 自带 `main()` 自测。写完跑通即掌握。

---

## 自测方法

Windows 下用 MinGW gcc 或 clang:

```
gcc linked_list.c -o linked_list && linked_list
```

或统一脚本: `gcc *.c -o 对应exe` (每个文件单独编译)。

**验收标准 (不过关不算掌握):**
1. 不看参考能手写出来
2. 能讲清每段的时间/空间复杂度
3. 能说出这题的边界坑 (空链表/空串/溢出)

---

## P0 — 必修, 嵌入式笔试超高频

### 1. linked_list.c
- 结构体定义 (单链表节点)
- 创建 / 头插 / 尾插 / 打印
- **反转链表** (迭代 + 递归两版) — 笔试最高频
- 快慢指针: 找中间节点 / 环检测 (Floyd)
- 合并两个有序链表
- 删除倒数第 N 个节点
- main 自测

### 2. string.c
- **手写 strlen / strcpy / strcmp / strcat** (不用库) — 八股必考
- 字符串反转 / 回文判断
- 手写 atoi (处理正负 + 溢出)
- main 自测

### 3. sort.c
- 冒泡 / 选择 / 插入
- **快排** (递归版, partition 函数)
- 归并
- **堆排序**
- 每个算法注释: 时间/空间复杂度 + 稳定性
- main 自测 (乱序数组验证升序)

### 4. binary_tree.c
- 结构体定义
- 前 / 中 / 后序 递归版
- 前 / 中 / 后序 迭代版 (手写栈) — 高频考迭代
- 层序遍历 (队列)
- 最大深度 / 节点数
- 镜像翻转
- main 自测

### 5. bit_ops.c
- 奇偶判断 / 置位 / 清位 / 翻转
- 判断 2 的幂
- 汉明权重 (数二进制 1 的个数)
- 异或交换 / 找只出现一次的数
- main 自测

---

## P1 — 必修, 常考

### 6. stack_queue.c
- 数组栈 (push/pop/top)
- 数组循环队列 (判空/判满)
- 用两个栈实现队列
- 括号匹配
- main 自测

### 7. memory_align.c — 八股重灾区, 笔试选择题 + 面试必问
- **结构体内存对齐验证** (offsetof / sizeof)
- 字节序检测 (大端/小端)
- **手写 memcpy / memmove** (重叠区区别) — 高频
- 指针运算 / 函数指针
- main 自测

### 8. hash.c
- 简单哈希表: 数组 + 链地址法
- 插入 / 查找 / 删除
- main 自测

### 9. heap.c
- 最小堆数组实现: 插入 / 取顶 / 堆化
- 用堆做堆排序 (可和 sort.c 交叉)
- main 自测

---

## P2 — 选修, 有余力再写

### 10. recursion.c
- 二分查找 (递归 + 迭代)
- 快速幂
- 汉诺塔 / 全排列
- main 自测

### 11. dp_basics.c
- 爬楼梯
- 最长递增子序列 (LIS)
- 0-1 背包
- main 自测

### 12. two_pointers.c
- 有序数组去重
- 两数之和 (排序后双指针)
- 最长无重复子串 (滑动窗口)
- main 自测

---

## 练习顺序

```
P0 (1-5) -> P1 (6-9) -> P2 (10-12, 按需)
```

**每日节奏 (副线, 不抢主线):**
- 每天 1 个 .c 文件, 1-1.5 小时
- P0 全部 + P1 全部 = 9 个文件, 约 10 天完成
- P2 挑短板写, 不追求全写

## 和笔试/面试的对应

| 文件 | 对应考点 |
|---|---|
| linked_list.c | 笔试编程题最高频; 面试手写代码 |
| string.c / memory_align.c | C 八股选择题 + 手写代码题 |
| sort.c | 排序复杂度/稳定性选择题 + 编程题 |
| binary_tree.c | 编程题 + 面试手撕 |
| bit_ops.c | 笔试选择题 + 嵌入式位操作场景 |
| stack_queue.c | 笔试编程题 (用栈实现队列等) |
| hash.c / heap.c | 编程题 (Top-K / 统计频率) |

**嵌入式专考点 (笔试试卷更爱考, 补进 memory_align.c):**
- 大小端 (判断 + 转换函数)
- 结构体对齐/字节对齐 (pragma pack)
- volatile / const / static 作用
- 指针数组 vs 数组指针
- 内存四区 (栈/堆/全局/代码段)
