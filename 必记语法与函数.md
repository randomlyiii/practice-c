# C 算法笔试必记语法与函数手册

> 面试 / 笔试速查手册。所有代码可直接复制使用。
> C 语言版（对应 C++ 版手册，STL 相关全部改成手写实现 + C 标准库）。

## 一、精准小数位的表达（printf）

> C 语言没有 setprecision / iomanip，只用 printf。`<stdio.h>` 已含。

### 1. printf 用法

> `%.nf` 表示**小数点后 n 位**。

```c
#include <stdio.h>

double pi = 3.14159265;

printf("%.2f\n", pi);   // 输出: 3.14
printf("%.4f\n", pi);   // 输出: 3.1416 （四舍五入）
printf("%.2f\n", 3.0 / 7);  // 输出: 0.43
```

**常用格式符对照**：

| 类型 | 格式符 | 示例 |
|------|--------|------|
| `int` | `%d` | `printf("%d", 42);` |
| `long` | `%ld` | `printf("%ld", 42L);` |
| `long long` | `%lld` | `printf("%lld", 42LL);` |
| `unsigned int` | `%u` | `printf("%u", 42u);` |
| `float` / `double` | `%f` | `printf("%.2f", 3.14);` |
| `long double` | `%Lf`（大写L） | `printf("%.2Lf", 3.14L);` |
| `char` | `%c` | `printf("%c", 'A');` |
| `char[]` 字符串 | `%s` | `printf("%s", s);` |
| `size_t` | `%zu` | `printf("%zu", strlen(s));` |
| 十六进制 int | `%x` / `%X` | `printf("%X", 255);` |
| 科学计数法 | `%e` | `printf("%e", 3.14);` |

**要点**：
- `%.nf` 的 `.n` 就是**小数点后 n 位**（`.2f` = 保留2位小数）。
- `float` 传入 printf 会自动提升成 `double`，所以 `%f` 通吃两者。
- 想按整数保留零头（如 3 位有效数字）用 `%g` 或手动处理，笔试多用 `%.nf` 即可。
- 换行用 `\n`，printf 里没有 `endl`。

### 2. scanf 输入

```c
//一定要记得加&
int n;
double x;
char c;
char s[100];          // 不含空格
scanf("%d %lf %c %s", &n, &x, &c, s);  // %lf 读 double，字符/字符串不用&
//C语言里没有string

// ⚠️ %c 不跳过空白：前面输入若残留换行符，会读成 '\n'，要配 getchar() 清掉
// 读一整行（含空格）：用 fgets，见第三节
```

---

## 必备头文件

```c
#include <stdio.h>    // printf / scanf / getchar
#include <string.h>   // strlen / strcpy / strcmp / strstr / memset
#include <stdlib.h>   // qsort / bsearch / malloc / atoi / abs
#include <math.h>     // sqrt / pow / fabs
#include <limits.h>   // INT_MAX / INT_MIN
#include <ctype.h>    // tolower / toupper
#include <stdbool.h>  // bool true false（可选，或用 int 0/1）
#include <stdint.h>   // uint8_t / uint32_t（嵌入式必用）
#include <stddef.h>   // offsetof
```

---

## int 取值范围

```
// int 有效值范围
// 32位有符号 int
INT_MIN  = -2147483648
INT_MAX  = 2147483647
范围：[-2147483648 , 2147483647]
模糊记忆（算法口算）
近似：-2*10^9  ~  +2*10^9
// 需要 <limits.h>
```

---

## C语言复杂变量定义（变量a）

### C语言指针、数组、函数指针经典笔试题目

|序号|定义语句|描述|注释|
|---|---|---|---|
|a|`int a;`|一个整型数|An integer|
|b|`int *a;`|一个指向整型数的指针|A pointer to an integer|
|c|`int **a;`|一个指向指针的指针，它指向的指针指向一个整型数|A pointer to a pointer to an integer|
|d|`int a[10];`|一个有10个整型数的数组|An array of 10 integers|
|e|`int *a[10];`|一个有10个指针的数组，该指针是指向一个整型数|An array of 10 pointers to integers<br>等价写法：`int *(a[10]);`|
|f|`int (*a)[10];`|一个指向有10个整型数数组的指针|A pointer to an array of 10 integers|
|g|`int (*a)(int);`|一个指向函数的指针，该函数有一个整型参数并返回一个整型数|A pointer to a function that takes an integer as an argument and returns an integer<br>等价写法：`int (*a)(int x);`|
|h|`int (*a[10])(int);`|一个有10个指针的数组，该指针指向一个函数，该函数有一个整型参数并返回一个整型数|An array of ten pointers to functions that take an integer argument and return an integer|

**关键辨析要点**
1. `[]` 的优先级高于 `*`
- `int *a[10]`：`a`先和`[10]`结合 → 数组，数组里面元素是`int*`（10个int指针的数组，对应e）
- `int (*a)[10]`：括号改变优先级，`a`先和`*`结合 → 指针，指针指向大小为10的int数组（对应f）

2. 函数指针 `()`优先级高于`*`
- `int (*a)(int)`：括号强制`a`和`*`结合，`a`是函数指针（对应g）
- `int (*a[10])(int)`：`a[10]`是数组，数组每一项是函数指针（对应h）

### 指针 others 1###
int a[5] = {1,2,3,4,5};
int *ptr = (&a + 1);
printf("%d,%d", *(a + 1), *(ptr - 1));
//2,5

**分步解析**
1. `a` 数组名，在表达式中退化为首元素地址 `&a[0]``a + 1`：指针步长是`int`，指向`a[1]``*(a+1)` → `a[1] = 2`
2. `&a`：取**整个数组**的地址，类型是 `int (*)[5]`（数组指针）`&a + 1`：偏移单位是**整个数组大小（5 个 int）**，跳到数组`a`后面的内存地址。
把这个地址强制赋值给`int *ptr`。
3. `ptr` 是`int*`类型，`ptr - 1` 步长为单个`int`，回退一格，指向`a[4]``*(ptr -1)` → `a[4] = 5`

**核心考点**:**
- `a+1`：移动**1 个 int**
- `&a+1`：移动**整个数组**，跨过全部 5 个 int

> 这题 `ptr = (&a+1)`，和之前 `int *p = (int *)(&a + 1);` 等价，这里隐式强制转换。

---

## 二、char[] 字符串操作（string.h）

> C 没有 string 类型，用 `char 数组` + `string.h` 函数。**所有修改类函数都需要数组足够大。**

### 1. 拼接操作

```c
char s1[100] = "abc";      // 必须预留空间，或 char s1[20]="abc"
char s2[] = "123";
strcat(s1, s2);            // 追加，s1 变成 "abc123"
strncat(s1, "def", 3);     // 追加前3个字符，s1 变成 "abc123def"
int len = strlen(s1);      // 字符串长度（不含结尾'\0'）
s1[len] = '!';             // 手动追加单个字符
s1[len + 1] = '\0';        // 别忘了补结束符！
```

### 2. 查找操作（重点）

```c
char s[] = "hello algorithm";
char *p = strstr(s, "alg");   // 返回匹配位置指针，找不到返回 NULL
if (p != NULL) {
    int pos = p - s;          // 指针减指针 = 下标（此处为6）
}

// 单字符查找：strchr（从前往后）/ strrchr（从后往前）
char *q = strchr(s, 'o');     // 第一个'o'，返回指针，下标4
char *r = strrchr(s, 'o');    // 最后一个'o'，下标9

// 循环查找所有出现
char text[] = "ABC DESC 123 DESC XYZ DESC end";
char *cur = text;
int cnt = 0;
while ((cur = strstr(cur, "DESC")) != NULL) {
    printf("找到DESC，起始下标：%d\n", (int)(cur - text));
    cnt++;
    cur += 4;        // 跳过本次匹配，避免重复匹配重叠
}
```

### 3. 截取、删除、替换

```c
// 截取：从下标6开始复制5个字符 -> "algor"
char s[] = "hello algorithm";
char sub[20];
strncpy(sub, s + 6, 5);
sub[5] = '\0';            // strncpy 不会自动补'\0'，必须手动！

// 删除：删 [6, 10) 的 "algo"，剩余部分整体前移 -> "hello rithm"
char s2[] = "hello algorithm";
memmove(s2 + 6, s2 + 10, strlen(s2) - 10 + 1); // 拷贝"rithm\0"共6字节
// 结果 s2 = "hello rithm"

// 替换：等长替换直接覆盖即可（无需移动）
char s3[] = "hello algorithm";
strncpy(s3 + 6, "code", 4);   // "algo" -> "code"，s3 = "hello coderithm"
// 变长替换：先 memmove 腾出空间，再 strncpy 写入
```

### 4. 其他常用

```c
int len = strlen(s);            // 字符串长度（不含'\0'）
int cmp = strcmp(a, b);         // a<b 负数 / a==b 0 / a>b 正数
int nc = strncmp(a, b, 3);      // 只比较前3个字符
int num = atoi("1234");         // 字符串转int（stdlib.h）
long num2 = atol("12345678");   // 转long
char str[20];
snprintf(str, sizeof(str), "%d", 1234);     // int 转字符串
snprintf(str, sizeof(str), "%.2f", 3.1415); // 万能格式化
// ⚠️ 用 snprintf 而非 sprintf：sprintf 无长度限制，缓冲区小就溢出（崩溃/漏洞）
// 缓冲区大小确定时用 snprintf(buf, sizeof(buf), fmt, ...) 最稳，笔试/工程通用
// 字符串转数字更安全方式：
int x;
sscanf("  42abc", "%d", &x);    // 从字符串按格式读，x = 42
```

---

## 三、fgets / scanf 整行读取（重点坑点）

```c
char line[1000];
fgets(line, sizeof(line), stdin);   // 读取一整行（含空格），会带末尾'\n'

// ⚠️ 经典坑1：scanf 读数字后残留换行符，fgets 会读到空行
// 正确写法：用 getchar() 吃掉缓冲区的换行
int n;
scanf("%d", &n);
getchar();               // 必须！清除 scanf 留下的换行符
fgets(line, sizeof(line), stdin);   // 正常读取一整行

// ⚠️ 经典坑2：fgets 带'\n'，去尾换行
size_t len = strlen(line);
if (len > 0 && line[len - 1] == '\n') line[len - 1] = '\0';

// 多行数字输入：scanf 直接读到 '\n' 或 EOF 为止
// while (scanf("%d", &x) != EOF) { ... }

// 字符输入：scanf 直接读到结束为止(读到EOF结束)，读取每一个字符（包含换行、空格）
// while (scanf("%c", &ch) == 1) { ... }
```

---

## 四、C 常用容器 / 数据结构手写实现

> 统一模式：**struct 只存数据，操作都是外部函数** `xxxPush(&v, x)`。最短、最好记。
> 能 `{0}` 零初始化的字段直接免 init（如 vector）；不能的（栈 top=-1、deque 中部基准）必须调 init。
> ⚠️ 四个静态版都是**栈上大数组**（~400KB），数据量接近 1e5 会栈溢出，改用堆版或 static/全局（详见 1. 节注）。
> 以下为 int 版本；泛型版把 `int` 换成 `void*` + `memcpy`（见节末注）。

### 1. 动态数组 vector

```c
#define VEC_CAP 100005
typedef struct {
    int data[VEC_CAP];
    int len;             // 元素个数
} Vec;

void vecInit(Vec *v)        { v->len = 0; }
void vecPush(Vec *v, int x) { v->data[v->len++] = x; }  // ⚠️ len 满越界
void vecPop(Vec *v)         { if (v->len > 0) v->len--; }
int  vecAt(Vec *v, int i)   { return v->data[i]; }
int  vecFront(Vec *v)       { return v->data[0]; }
int  vecBack(Vec *v)        { return v->data[v->len - 1]; }
int  vecSize(Vec *v)        { return v->len; }
int  vecEmpty(Vec *v)       { return v->len == 0; }
void vecClear(Vec *v)       { v->len = 0; }

// 用法
Vec v = {0};                // 零初始化，len 自动为 0，免 vecInit
vecPush(&v, 5);
vecPush(&v, 9);
int x = vecAt(&v, 0);       // 5
int n = vecSize(&v);        // 2
for (int i = 0; i < vecSize(&v); i++) printf("%d ", vecAt(&v, i));
```

> ⚠️ **栈溢出坑**：`Vec v;` 是栈上局部变量，`data[100005]` ≈ 400KB。题目数据量接近 1e5 时栈直接爆。
> 笔试正确做法：
> ① 优先**堆版 DynVec**（malloc/realloc，见下方）；
> ② 或 `static Vec v;` / 全局变量 —— 进 .bss 不占栈；
> ③ 或按数据量缩小 `VEC_CAP`。

**动态扩容版**（自动增长，等价 C++ vector，需 `<stdlib.h>`）：

```c
typedef struct {
    int *data;
    int len, cap;
} DynVec;

void dynInit(DynVec *v) { v->data = NULL; v->len = v->cap = 0; }
void dynPush(DynVec *v, int x) {
    if (v->len == v->cap) {
        v->cap = v->cap ? v->cap * 2 : 4;                  // 容量翻倍
        v->data = (int *)realloc(v->data, v->cap * sizeof(int));
    }
    v->data[v->len++] = x;
}
void dynFree(DynVec *v) { free(v->data); v->data = NULL; v->len = v->cap = 0; }
```

### 2. 栈 stack

```c
#define STACK_CAP 100005
typedef struct {
    int data[STACK_CAP];
    int top;             // 栈顶元素下标，栈空时 top = -1
} Stack;

void stackInit(Stack *s)  { s->top = -1; }      // ⚠️ top 初始 -1，不能靠 {0}
int  stackEmpty(Stack *s) { return s->top == -1; }
int  stackSize(Stack *s)  { return s->top + 1; }
void stackPush(Stack *s, int x) { s->data[++s->top] = x; }  // ⚠️ 满栈越界
int  stackPop(Stack *s)   { return s->data[s->top--]; }     // ⚠️ 空栈先判 stackEmpty
int  stackTop(Stack *s)   { return s->data[s->top]; }

// 用法
Stack st;
stackInit(&st);
stackPush(&st, 10);
stackPush(&st, 20);
int t = stackPop(&st);            // 20
int p = stackTop(&st);            // 10
```

### 3. 队列 queue（环形缓冲，避免整体搬移）

```c
#define QUEUE_CAP 100005
typedef struct {
    int data[QUEUE_CAP];
    int head, tail;      // [head, tail) 半开区间，环形回绕
    int count;           // 元素个数（环形判满必需）
} Queue;

void queueInit(Queue *q)        { q->head = q->tail = q->count = 0; }
int  queueEmpty(Queue *q)       { return q->count == 0; }
int  queueFull(Queue *q)        { return q->count == QUEUE_CAP; }
int  queueSize(Queue *q)        { return q->count; }
void queuePush(Queue *q, int x) {
    if (queueFull(q)) return;                        // 满队不入
    q->data[q->tail] = x;
    q->tail = (q->tail + 1) % QUEUE_CAP;             // 环形回绕
    q->count++;
}
int  queuePop(Queue *q) {
    int x = q->data[q->head];
    q->head = (q->head + 1) % QUEUE_CAP;
    q->count--;
    return x;
}
int  queueFront(Queue *q) { return q->data[q->head]; }
int  queueBack(Queue *q)  { return q->data[(q->tail - 1 + QUEUE_CAP) % QUEUE_CAP]; }

// 用法
Queue q = {0};              // head/tail/count 全 0，正好是空队，免 init
queuePush(&q, 10);
queuePush(&q, 20);
int f = queueFront(&q);     // 10
int b = queueBack(&q);      // 20
int d = queuePop(&q);       // 10
```

### 4. 双端队列 deque

```c
#define DEQUE_CAP 100005
#define DEQUE_MID  50000          // 中部基准，给头插留空间
typedef struct {
    int data[DEQUE_CAP];
    int l, r;             // [l, r] 闭区间，l 队首 r 队尾
} Deque;

void dequeInit(Deque *d) { d->l = DEQUE_MID; d->r = DEQUE_MID - 1; }  // ⚠️ 不能靠 {0}
int  dequeEmpty(Deque *d) { return d->l > d->r; }
int  dequeSize(Deque *d)  { return d->r - d->l + 1; }
void dequePushBack(Deque *d, int x)  { d->data[++d->r] = x; }  // ⚠️ r 到头越界
void dequePushFront(Deque *d, int x) { if (d->l > 0) d->data[--d->l] = x; } // ⚠️ l=0 越界
int  dequePopBack(Deque *d)  { return d->data[d->r--]; }
int  dequePopFront(Deque *d) { return d->data[d->l++]; }
int  dequeFront(Deque *d) { return d->data[d->l]; }
int  dequeBack(Deque *d)  { return d->data[d->r]; }

// 用法
Deque dq;
dequeInit(&dq);
dequePushBack(&dq, 1);
dequePushFront(&dq, 0);
int f = dequeFront(&dq);          // 0
int b = dequeBack(&dq);           // 1
```

> **泛型化**：把 `int` 换成 `void*`（元素存指针），增删用 `memcpy` + 元素 `sizeof`。笔试多为 int，按需扩展。

---

## 五、常用算法函数（stdlib.h 的 qsort / 手写二分）

### 1. qsort 排序（重点）

> 需要**自己写比较函数**。升序返回值负→a排前；相等0；正→b排前。

```c
#include <stdlib.h>

// int 数组升序
int cmp_int(const void *a, const void *b) {
    return *(int *)a - *(int *)b;          // 升序
}
// 降序：return *(int *)b - *(int *)a;
int arr[] = {3, 1, 4, 2, 5};
int n = sizeof(arr) / sizeof(arr[0]);
qsort(arr, n, sizeof(int), cmp_int);       // 1 2 3 4 5

// 结构体按某个字段排序（贪心题高频）
typedef struct { int w, v; } Item;
int cmp_item(const void *a, const void *b) {
    const Item *x = a, *y = b;
    return x->v - y->v;                    // 按 v 升序
}
Item items[1005];
qsort(items, n, sizeof(Item), cmp_item);

// 字符串数组排序：strcmp 形参是 const char*，不能直接当 qsort 比较函数，包一层
int cmp_str(const void *a, const void *b) {
    return strcmp(*(char *const *)a, *(char *const *)b);
}
char *words[1005];
qsort(words, n, sizeof(char *), cmp_str);  // 按字典序升序
```

**要点**：
- 比较函数入参是 `const void *`，必须强转，不能直接用 `int*`。
- 用 `x->v - y->v` 可能溢出（两个大正数相减），笔试可用
  `return (x->v > y->v) - (x->v < y->v);` 规避。

### 2. 手写二分 lower_bound / upper_bound（重点，需先排序）

- `lower_bound`: 第一个 **>= val** 的下标
- `upper_bound`: 第一个 **> val** 的下标
- 找不到时返回数组长度 n

```c
// 第一个 >= val 的下标（左闭右开 [0, n)，返回 n 表示不存在）
int lower_bound(int a[], int n, int val) {
    int l = 0, r = n;
    while (l < r) {
        int mid = l + (r - l) / 2;   // 防 l+r 溢出
        if (a[mid] < val) l = mid + 1;
        else r = mid;
    }
    return l;   // 下标；== n 表示不存在
}
// 第一个 > val 的下标：把上面 if 条件改成 a[mid] <= val

// 经典用法：统计 val 出现次数 = upper_bound - lower_bound
// 判断是否存在：
// if (lower_bound(a, n, val) < n && a[lower_bound(a, n, val)] == val)

// 也可以直接用 bsearch（stdlib.h），但比较函数返回值方向相反，手写更可控
```

### 3. 去重（排序后手动去重）

```c
qsort(a, n, sizeof(int), cmp_int);
int m = 0;                             // 新长度
for (int i = 0; i < n; i++)
    if (i == 0 || a[i] != a[m - 1])
        a[m++] = a[i];                 // 去重后前 m 个就是答案
```

### 4. 全排列 next_permutation（手写，需先升序 + 先处理初始排列）

```c
// 原地生成下一个字典序排列，返回 1 表示有下一个，0 表示已经是最后一个
int next_permutation(int a[], int n) {
    int i = n - 2;
    while (i >= 0 && a[i] >= a[i + 1]) i--;   // 找第一个升序对
    if (i < 0) return 0;                      // 已是最大排列
    int j = n - 1;
    while (a[j] <= a[i]) j--;                 // 找右边第一个比 a[i] 大的
    int t = a[i]; a[i] = a[j]; a[j] = t;
    // 反转 i+1 .. n-1
    for (int l = i + 1, r = n - 1; l < r; l++, r--) {
        t = a[l]; a[l] = a[r]; a[r] = t;
    }
    return 1;
}
// 用法：
qsort(a, n, sizeof(int), cmp_int);   // 先升序
do {
    // 处理当前排列 a
} while (next_permutation(a, n));
```

---

## 六、快速幂（n次幂快速运算，带取模）

> 计算 `(a^b) % mod`，时间复杂度 O(log b)

```c
long long qpow(long long a, long long b, long long mod) {
    long long res = 1;
    a %= mod;
    while (b > 0) {
        if (b & 1) { // 二进制最低位为1
            res = res * a % mod;
        }
        a = a * a % mod;
        b >>= 1; // 右移一位，等价于b /= 2
    }
    return res;
}
// 调用示例：计算 2^10 % 1000 = 24
// long long ans = qpow(2, 10, 1000);
```

---

## 七、补充常用工具函数

```c
// 1. 最大公约数（GCD）
int gcd(int a, int b) {
    return b == 0 ? a : gcd(b, a % b);
}

// 2. 最小公倍数（LCM）
int lcm(int a, int b) {
    return a / gcd(a, b) * b;
}

// 3. 数组批量初始化（memset，按字节赋值，<string.h>）
int arr[100];
memset(arr, 0, sizeof(arr));   // 全部置0
memset(arr, -1, sizeof(arr));  // 全部置-1
// ⚠️ 只能置 0 / -1，其他值（如1）按字节填会出错

// 4. 字符大小写转换（<ctype.h>）
char c = 'A';
c = tolower((unsigned char)c); // 参数转 unsigned char，防负值 UB（非ASCII字符）
c = toupper((unsigned char)c); // 转大写 'A'

// 5. 动态分配（需要变长数组时，<stdlib.h>，n 需提前定义）
int *v = (int *)malloc(n * sizeof(int));  // 等价 new int[n]
if (v == NULL) { /* 分配失败处理（嵌入式小内存常发生）*/ }
free(v);                                  // 用完释放！
// 笔试大多固定数组 + memset，malloc 少见但要知道 free
```

---

## 八、位运算（高频技巧）

> `__builtin_*` 是 gcc / clang 内建函数，牛客 / 力扣 C 环境均可用。非 gcc 环境可手写。

```c
int x = 12; // 二进制 1100
int c1 = __builtin_popcount(x);   // 二进制中1的个数 = 2
long long y = 12LL;
int c2 = __builtin_popcountll(y); // long long版
int k = __builtin_ctz(x);         // 末尾连续0个数 = 2（即最低位1在第2位）

// 判断第 k 位是否为1（从0开始）
bool isOne = (x >> k) & 1;
// 取最低位的1（lowbit，树状数组/状态压缩常用）
int lowbit = x & (-x);            // 12 & -12 = 4 (100)

// 常见位运算
x <<= 1;    // 左移1位 = x * 2
x >>= 1;    // 右移1位 = x / 2（负数右移实现相关，常见算术右移）
int andV = a & b;   // 与
int orV  = a | b;   // 或
int xorV = a ^ b;   // 异或（相同为0，不同为1）
int notV = ~a;      // 取反
// 判断 x 是否是2的幂： x > 0 && (x & (x - 1)) == 0
// 手写 popcount（非gcc环境）：
int popcount(int x) { int c = 0; while (x) { x &= x - 1; c++; } return c; }
```

---

## 九、嵌入式 C 必记要点（面试高频）

> 单片机 / 嵌入式笔试面试高频。核心：**volatile、寄存器位操作、const 与指针、结构体对齐、宏、内存模型、原子操作**。
> 本节代码需要 `#include <stdint.h>`（uint8_t / uint32_t）、`<stddef.h>`（offsetof）；原子节需 `<stdatomic.h>`（C11）。

### 1. volatile（嵌入式第一关键字）

> 告诉编译器该变量随时可能被外部改变，**不要优化掉**读写。硬件寄存器、中断共享变量、多线程共享变量必须 volatile。

```c
// 寄存器等待标志位（不 volatile 编译器可能优化成死循环）
volatile unsigned int *STATUS = (volatile unsigned int *)0x40021000;
while ((*STATUS & 0x01) == 0) { /* 等待硬件置位 */ }

// 中断置位，主循环轮询
volatile int g_flag = 0;
// 中断服务函数里: g_flag = 1;
while (g_flag == 0) { /* 等中断 */ }
```

**为什么**：编译器优化会把 `while((*STATUS&1)==0)` 当成每次值不变，只读一次就死循环；volatile 强制每次都真正读内存/寄存器。

### 2. 寄存器位操作三件套（宏）

```c
#define SET_BIT(reg, bit)    ((reg) |= (1U << (bit)))   // 置1
#define CLEAR_BIT(reg, bit)  ((reg) &= ~(1U << (bit))) // 清0
#define TOGGLE_BIT(reg, bit) ((reg) ^= (1U << (bit)))  // 翻转
#define GET_BIT(reg, bit)    (((reg) >> (bit)) & 1U)   // 读

// 直接定义寄存器地址（地址转指针，必须 volatile + 强制转换）
#define RCC_APB2ENR  (*(volatile uint32_t *)0x40021018U)
#define GPIOB_CRH    (*(volatile uint32_t *)0x40010C04U)

// 用法
SET_BIT(RCC_APB2ENR, 3);        // 使能时钟位
CLEAR_BIT(GPIOB_CRH, 20);       // 清位
```

### 3. const 与指针（嵌入式笔试必考辨析）

| 写法 | 含义 | 可改性 |
|------|------|--------|
| `const int *p` | 指针指向常量 | `*p` 不可改，`p` 可改 |
| `int *const p` | 指针常量 | `p` 不可改，`*p` 可改 |
| `const int *const p` | 两者都不可改 | 都不可改 |
| `int const *p` | 等价 `const int *p` | 同第一行 |

```c
const int *p1;      // *p1 只读
int *const p2;      // p2 不能指向别处
const int *const p3; // 全锁定

// 口诀：const 修饰谁看它左边（左边没有就看右边）
// const int *p -> *p 只读；int *const p -> p 只读
```

**嵌入式典型用途**：
```c
// 只读寄存器表 / 常量表放 flash，省 RAM
const uint8_t lookup_table[16] = {...};
// 函数入参只读保护
void send(const uint8_t *data, uint16_t len);
```

### 4. static / extern / 作用域（嵌入式高频）

```c
// static 局部变量：静态存储，只初始化一次，跨函数调用保持值
int counter() {
    static int cnt = 0;    // 编译期分配，每次调用不重置
    return ++cnt;          // 1,2,3...
}

// static 全局变量 / static 函数：只在本文件可见（限制作用域，防污染）
static int s_internal;          // 其他 .c 无法 extern 引用
static void helper(void) {...}  // 本文件私有函数

// extern：引用其他文件定义（变量必须别处已定义且非 static）
extern int g_global;            // 声明，不定义
```

**嵌入式意义**：多 .c 文件工程，全局变量用 static 限制可见范围；中断和主循环共享的用 volatile。

### 5. 结构体对齐与 packed（通信协议解析必用）

```c
// 默认对齐：编译器会按最大成员填充字节
typedef struct {
    uint8_t type;      // 1字节
    uint32_t len;      // 4字节
} Msg;   // 默认 sizeof = 8（type后填充3字节）

// 强制紧凑打包（去掉填充，用于直接映射协议帧）
#pragma pack(push, 1)
typedef struct {
    uint8_t type;
    uint32_t len;
} MsgPacked;           // sizeof = 5
#pragma pack(pop)

// 或 GCC 单结构体写法
typedef struct __attribute__((packed)) {
    uint8_t type;
    uint32_t len;
} MsgPacked2;          // sizeof = 5

// 取成员偏移量（需 <stddef.h>）
int off = offsetof(MsgPacked, len);   // = 1
```

**要点**：收发协议帧、flash 存储结构体、内存映射寄存器结构体时用 packed，避免填充字节导致数据错位。

### 6. 位域（bit-field，压缩到1字节）

```c
typedef struct {
    unsigned char bit0 : 1;   // 占1位
    unsigned char bit1 : 1;
    unsigned char rsv  : 6;   // 预留6位
} Flags;   // sizeof = 1（全部压进1字节）

Flags f;
f.bit0 = 1;
if (f.bit1) { ... }
```

**注意**：位域跨编译器/大小端有差异，通信协议层谨慎用，寄存器操作多用宏位运算更可移植。

### 7. 函数指针 / 回调（中断向量表、状态机、协议分发）

```c
// 函数指针类型
typedef void (*Handler_t)(void);

void timer_isr(void) { ... }
void uart_isr(void)  { ... }

Handler_t handlers[8];
handlers[0] = timer_isr;
handlers[1] = uart_isr;
handlers[0]();   // 通过指针调用

// 回调：把处理函数传给底层驱动
void register_handler(Handler_t h) { ... }
```

### 8. 宏 vs 函数 vs inline（嵌入式折衷）

```c
// 函数式宏：参数必须加括号，防优先级坑
#define SQUARE(x) ((x) * (x))      // SQUARE(1+2) = 9，不加括号会算成 1+2*1+2
#define MAX(a, b) ((a) > (b) ? (a) : (b))

// 多语句宏：do-while(0) 包裹，避免 if-else 悬挂
#define LOG_ENABLE
#ifdef LOG_ENABLE
    #define LOG(fmt, ...) printf(fmt, ##__VA_ARGS__)  // ## 吃逗号是 GNU 扩展
#else
    #define LOG(fmt, ...)   // 空实现，编译期直接删掉
#endif

// 嵌入式推荐：static inline 替代小函数（无调用开销，又安全）
static inline int clamp(int x, int lo, int hi) {
    return x < lo ? lo : (x > hi ? hi : x);
}
```

### 9. 条件编译 / 头文件防重

```c
// 头文件防重复包含（Header Guard）
// ⚠️ 别用 __ 开头的名字（如 __XX_H_），__ 保留给编译器实现，用普通名字
#ifndef MY_MODULE_H
#define MY_MODULE_H
// 内容
#endif

// 平台/功能开关
#ifdef STM32F4XX
    // F4 代码
#elif defined(STM32F1XX)
    // F1 代码
#else
    #error "unknown MCU"
#endif

// 调试宏
#if 0   // 临时禁用一段代码
    ...
#endif
```

### 10. 大小端（字节序，通信/协议高频）

```c
// 判断本机大小端
int x = 0x12345678;
if (*(unsigned char *)&x == 0x78)
    printf("小端 Little-Endian\n");   // 低地址存低位，x86/ARM默认
else
    printf("大端 Big-Endian\n");      // 低地址存高位，网络字节序

// 协议收发：多字节数值先转网络序再发
// 小端数据按字节拆分发送：
uint32_t v = 0x12345678;
uint8_t buf[4] = { v & 0xFF, (v>>8) & 0xFF, (v>>16) & 0xFF, (v>>24) & 0xFF };
```

### 11. sizeof 陷阱（嵌入式面试常考）

```c
char str[] = "hello";   // sizeof(str) = 6（含'\0'）
char *p   = "hello";    // sizeof(p) = 4 或 8（指针大小，不是字符串长度）
                        // strlen(p) = 5（不含'\0'）

int arr[10];
sizeof(arr)          // 40
sizeof(arr[0])       // 4
// 数组名作函数参数退化成指针：
void f(int a[]) { sizeof(a); }   // = 8，不是数组大小！必须另传长度
```

### 12. 内存与 malloc（嵌入式谨慎）

```c
// 裸机/小内存 MCU：慎用 malloc/free（堆碎片、耗时不确定）
// 多用于静态数组 + 环形缓冲区
static uint8_t rx_buf[1024];   // 未初始化 static → 进 .bss，不占栈

// 栈很小：大数组、结构体别放栈，用 static 或全局
// 常见存储分布（编译后）：
//   .text  代码段（flash）
//   .data  已初始化全局变量（RAM）
//   .bss   未初始化全局变量（RAM）
//   .stack 栈（局部变量，大小有限，溢出会跑飞）
//   .heap  堆（malloc）
```

### 13. 断言 assert（参数/状态检查）

```c
#include <assert.h>
void uart_send(uint8_t *p) {
    assert(p != NULL);   // 非法参数立即暴露
    ...
}

// 嵌入式常自定义更强断言：出错进 while(1) 便于调试
#define ASSERT(cond) do { \
    if (!(cond)) { \
        /* 记录错误标志 / 打印 */ \
        while (1); \
    } \
} while (0)
```

### 14. 裸机主循环 + 中断 编程模型（面试高频）

```c
// 裸机标准结构：初始化 + 死循环轮询，中断异步处理
volatile uint32_t g_ms_tick = 0;

void SysTick_Handler(void) {   // 1ms 中断
    g_ms_tick++;               // ⚠️ ++ 非原子；volatile 只阻止编译器优化，不保证原子性
                               //    主循环读 tick 前先关中断（或原子读），读后再开
}

int main(void) {
    // 时钟 / GPIO / 外设初始化...
    while (1) {
        // 主循环只做非实时、可被中断打断的任务
        if (/* 有按键事件 */) { ... }
        // 避免在主循环里死等硬件标志（用轮询 + 超时）
    }
}
```

### 15. C11 原子操作（多线程/中断共享数据）

> volatile 只防编译器优化，**不保证原子性**——计数、标志、队列指针这类 read-modify-write 仍会丢更新，用 `<stdatomic.h>`（需 C11：`gcc -std=c11`）。

```c
#include <stdatomic.h>

atomic_int cnt = ATOMIC_VAR_INIT(0);
atomic_fetch_add(&cnt, 1);        // 原子自增（= cnt++ 但安全）
int v = atomic_load(&cnt);        // 原子读（默认 seq_cst）
atomic_store(&cnt, 10);           // 原子写

// atomic_flag 自旋锁（最简单的互斥）
atomic_flag lock = ATOMIC_FLAG_INIT;
void lock_acquire(void) {
    while (atomic_flag_test_and_set(&lock)) { /* 自旋等待 */ }
}
void lock_release(void) {
    atomic_flag_clear(&lock);
}

// CAS：比较并交换——保证"只执行一次"（一次性初始化 / 状态切换）
atomic_int started = ATOMIC_VAR_INIT(0);
int expected = 0;
if (atomic_compare_exchange_strong(&started, &expected, 1)) {
    // 只有赢得竞争的线程走到这里，其他线程 expected 已是 1 进不来
}
```

**memory_order 内存序**：

| 内存序 | 作用 | 使用 |
|--------|------|------|
| `memory_order_relaxed` | 只保证原子性，不保证顺序 | 计数器、仅自己关心的值 |
| `memory_order_acquire` | 配合 release：读到发布前的数据全部可见 | 读方 |
| `memory_order_release` | 配合 acquire：写完后对方 acquire 读可见 | 写方 |
| `memory_order_seq_cst` | 默认，全程序一致顺序，最安全也最慢 | 拿不准就用它 |

### 16. 无锁环形队列（SPSC，单生产者单消费者）

> 中断/生产者线程写，主循环/消费者线程读，无锁、无阻塞。**只适合单生产者单消费者**；多生产者/多消费者需 CAS 或加锁。

```c
#include <stdatomic.h>
#include <stdlib.h>
#include <stdbool.h>

// 环形队列结构体
typedef struct {
    int *buffer;       // 数据缓冲区
    int capacity;      // 容量（实际可用 capacity-1）
    atomic_int head;   // 出队位置，原子变量
    atomic_int tail;   // 入队位置，原子变量
} RingQueue;

// 创建队列
RingQueue *ring_queue_create(int capacity) {
    RingQueue *q = (RingQueue *)malloc(sizeof(RingQueue));
    q->capacity = capacity;
    q->buffer = (int *)malloc(sizeof(int) * capacity);
    atomic_init(&q->head, 0);
    atomic_init(&q->tail, 0);
    return q;
}

// 入队（只允许一个生产者调用）
bool ring_queue_push(RingQueue *q, int value) {
    int tail = atomic_load_explicit(&q->tail, memory_order_relaxed);
    int next_tail = (tail + 1) % q->capacity;
    if (next_tail == atomic_load_explicit(&q->head, memory_order_acquire))
        return false;                             // 满
    q->buffer[tail] = value;                      // 先写数据
    atomic_store_explicit(&q->tail, next_tail, memory_order_release); // 再移指针
    return true;
}

// 出队（只允许一个消费者调用）
bool ring_queue_pop(RingQueue *q, int *out_value) {
    int head = atomic_load_explicit(&q->head, memory_order_relaxed);
    if (head == atomic_load_explicit(&q->tail, memory_order_acquire))
        return false;                             // 空
    *out_value = q->buffer[head];                 // 先读数据
    atomic_store_explicit(&q->head, (head + 1) % q->capacity, memory_order_release); // 再移指针
    return true;
}
```

**用法**：

```c
RingQueue *q = ring_queue_create(1024);

// 中断 / 生产者线程：往里塞
ring_queue_push(q, 42);

// 主循环 / 消费者线程：取出来处理
int val;
if (ring_queue_pop(q, &val)) { /* 处理 val */ }
```

**要点**：
- 留一个槽位不用（实际容量 capacity-1）：`head == tail` 判空，`next_tail == head` 判满，无需计数。
- release/acquire 成对：写方先写数据、release 移指针；读方 acquire 读到指针后，数据必已可见。防止"指针先动、数据没写完"的竞态。
- **只能 SPSC**。多生产者/多消费者并发入队/出队会相互覆盖指针，必须加锁或 CAS。
- 中断里别用自旋锁（会死等）；中断→主循环传数据用这种无锁队列最合适。
- 需 C11，编译 `gcc -std=c11`。

---

### 17. 环形队列=保证线程安全（SPSC，单生产者单消费者）

```c
#include <stdlib.h>
#include <stdbool.h>
#include <pthread.h>
#include <stdint.h>
#define BUF_SIZE 64
// 环形队列结构体
typedef struct {
    uint8_t *buffer;      // 数据缓冲区
    uint16_t head;        // 出队位置（读指针）
    uint16_t tail;        // 入队位置（写指针）
    pthread_mutex_t mutex;
} ringbuf;

//初始化
void ringbuf_init(ringbuf *rb){
    if(rb == NULL)
        return;
    rb->buffer = malloc(BUF_SIZE);
    rb->head = 0;
    rb->tail = 0;
    pthread_mutex_init(&rb->mutex, NULL);
}

//写入 成功返回1，失败返回0
int ringbuf_push(ringbuf *rb, uint8_t data){
    if(rb == NULL || rb->buffer == NULL)
        return 0;
    int ret = 0;
    pthread_mutex_lock(&rb->mutex);
    // 满判断：牺牲1格区分空满
    if( ((rb->tail + 1) % BUF_SIZE) != rb->head ){
        rb->buffer[rb->tail] = data;
        rb->tail = (rb->tail + 1) % BUF_SIZE;
        ret = 1;
    }
    pthread_mutex_unlock(&rb->mutex);
    return ret;
}

//读取 成功返回1，失败返回0
int ringbuf_pop(ringbuf *rb, uint8_t *data){
    if(rb == NULL || rb->buffer == NULL || data == NULL)
        return 0;
    int ret = 0; // 修复原代码笔误 int ret -0;
    pthread_mutex_lock(&rb->mutex);
    if( rb->head != rb->tail ) //非空
    {
        *data = rb->buffer[rb->head];
        rb->head = (rb->head + 1) % BUF_SIZE;
        ret = 1;
    }
    pthread_mutex_unlock(&rb->mutex);
    return ret;
}

//获取队列大小
int ringbuf_count(ringbuf *rb){
    if(rb == NULL)
        return 0;
    int cnt = 0;
    pthread_mutex_lock(&rb->mutex);
    if(rb->tail >= rb->head){
        cnt = rb->tail - rb->head;
    }
    else{
        cnt = (BUF_SIZE - rb->head) + rb->tail;
    }
    pthread_mutex_unlock(&rb->mutex);
    return cnt;
}
```

---

## 附录：C++ 与 C 语法对照速查

| 功能 | C++ | C |
|------|-----|---|
| 字符串类型 | `std::string` | `char[]` + string.h 函数 |
| 字符串拼接 | `s1 + s2` / `append` | `strcat` / `strncat` |
| 查找子串 | `s.find(...)` | `strstr`，下标 = 指针差值 |
| 长度 | `s.size()` / `s.length()` | `strlen(s)` |
| 转 int | `stoi(s)` | `atoi(s)` / `sscanf` |
| 转字符串 | `to_string(n)` | `snprintf(str, sizeof(str), "%d", n)` |
| 整行读入 | `getline(cin, s)` | `fgets(s, len, stdin)` |
| 容器 | `vector/queue/stack/set/map/...` | 数组 + 手写（见第四节） |
| 排序 | `sort(begin, end, cmp)` | `qsort(arr, n, sizeof, cmp)` |
| 二分 | `lower_bound/upper_bound` | 手写（见第五节） |
| 布尔类型 | `bool` 内建 | `<stdbool.h>` 或 `int 0/1` |
| 空指针 | `nullptr` | `NULL` |
| 内存分配 | `new` / `delete` | `malloc` / `free` |
| 匿名比较 | lambda | 具名比较函数 |
